/// <reference types="vite/client" />

// Tipos para `import.meta.env` en proyectos Vite.
// Añade variables de entorno personalizadas aquí si las necesitas, por ejemplo:
// interface ImportMetaEnv { VITE_API_URL: string }
// interface ImportMeta { readonly env: ImportMetaEnv }
